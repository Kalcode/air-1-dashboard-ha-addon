# API Base URL for development with mock server
# Trailing slash added automatically by config
# In production, this is empty to use relative paths without leading slash
PUBLIC_API_BASE_URL=http://localhost:8099
